# Architecture Context — ckSearch

> Owned by humans + the `planner` agent. Keep this current — a stale
> architecture doc is worse than none.

## Stack
TODO: languages, frameworks, search engine (Elasticsearch/OpenSearch/
Typesense/...), datastores, infra.

## System overview
TODO: How a query flows: client → API → query layer → index. How indexing
works: source data → pipeline → index.

## Key modules
| Path | Responsibility |
|---|---|
| TODO | TODO |

## External dependencies & contracts
TODO: APIs consumed/exposed, schemas that other teams depend on.

## Known constraints
TODO: latency budgets, index size, rate limits, things that look refactorable
but aren't.
