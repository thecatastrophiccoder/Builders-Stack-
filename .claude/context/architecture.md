# Architecture Context — Builders' Stack

> Owned by humans + the `planner` agent. Keep this current — a stale
> architecture doc is worse than none. (Existing codebase? `/adopt`
> derives this from manifests and entry points.)

## Stack
TODO: languages, frameworks, datastores, infra, third-party services.

## System overview
TODO: The main flows end to end — how a request travels, how data gets in,
where state lives.

## Key modules
| Path | Responsibility |
|---|---|
| TODO | TODO |

## External dependencies & contracts
TODO: APIs consumed/exposed, schemas other teams depend on.

## Known constraints
TODO: performance budgets, scale limits, rate limits, and things that look
refactorable but aren't (say why).
